<!DOCTYPE html>
<html>
  <head>
    <title>FoodRecipeApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <main class="flex flex-col">
      <nav class="flex item-center justify-between w-full p-6 bg-slate-700">
        <div class="flex items-center">
          <%= link_to "RecipeApp", root_path, class: "text-2xl font-bold" %>
        </div>

        <div class="flex items-center">
          <% if user_signed_in? %>
              <%= link_to "Foods", foods_path, class: "ml-4" %>
              <%= link_to "Recipes", recipes_path, class: "ml-4" %>
              <%= link_to "Public Recipes", public_recipes_path, class: "ml-4" %>
              <%= link_to "Shopping List", shopping_list_path, class: "ml-4" %>
          <% end %>
        </div>

        <div class="flex items-center">
          <% if current_user %>
            <%= button_to "Sign out", destroy_user_session_path, method: :delete, class: "ml-4 bg-white text-black p-2 rounded-md hover:bg-red-400" %>

          <% end %>
        </div>
      </nav>

      <p class="notice"><%= notice %></p>
      <p class="alert"><%= alert %></p>

      <div class="px-10 mt-6">
        <%= yield %>
      </div>
    </main>
  </body>
</html>
